import React from 'react';
interface MessageEditorProps {
    messageId: number;
    initialContent: string;
}
declare const MessageEditor: React.FC<MessageEditorProps>;
export default MessageEditor;
