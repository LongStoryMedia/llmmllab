# Code generated by schema2code at 2026-01-21 18:11:21. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from .complexity_level import ComplexityLevel
from .computational_requirement import ComputationalRequirement
from .document import Document
from .intent_analysis import IntentAnalysis
from .message_content import MessageContent
from .message_content_type import MessageContentType
from .message_role import MessageRole
from .required_capability import RequiredCapability
from .resource_usage import ResourceUsage
from .response_format import ResponseFormat
from .technical_domain import TechnicalDomain
from .thought import Thought
from .tool_call import ToolCall
from .workflow_type import WorkflowType
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class Message(BaseModel):
    """Message represents a single exchange in the conversation"""
    role: MessageRole = Field(..., description="Role of the message sender (e.g., user, assistant)")
    """Role of the message sender (e.g., user, assistant)"""
    content: List[MessageContent] = Field(..., description="Content of the message")
    """Content of the message"""
    id: Optional[int] = Field(default=None, description="Unique identifier for the message")
    """Unique identifier for the message"""
    created_at: Optional[datetime] = Field(default=None, description="Timestamp of when the message was created")
    """Timestamp of when the message was created"""
    conversation_id: Optional[int] = Field(default=None, description="Unique identifier for the conversation")
    """Unique identifier for the conversation"""
    thoughts: Optional[List[Thought]] = Field(default=None, description="Array of thoughts or reasoning processes from the assistant")
    """Array of thoughts or reasoning processes from the assistant"""
    tool_calls: Optional[List[ToolCall]] = Field(default=None, description="Array of tool calls associated with the message")
    """Array of tool calls associated with the message"""
    analyses: Optional[List[IntentAnalysis]] = Field(default=None, description="Array of intent analyses associated with the message")
    """Array of intent analyses associated with the message"""
    documents: Optional[List[Document]] = Field(default=None, description="Array of documents attached to the message")
    """Array of documents attached to the message"""
    structured_output: Optional[Dict[str, Any]] = Field(default=None, description="Structured output data from the message")
    """Structured output data from the message"""

    class Config:
        extra = "ignore"