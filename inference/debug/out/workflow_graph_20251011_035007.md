```mermaid
---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	intent_analysis(intent_analysis)
	workflow_router(workflow_router)
	engineering_agent(engineering_agent)
	title_generation(title_generation)
	memory_search(memory_search)
	memory_creation(memory_creation)
	memory_storage(memory_storage)
	static_tool_collection(static_tool_collection)
	dynamic_tool_collection(dynamic_tool_collection)
	tool_composer(tool_composer)
	tool_executor(tool_executor)
	chat_summary(chat_summary)
	search_summary(search_summary)
	chat_agent(chat_agent)
	__end__([<p>__end__</p>]):::last
	__start__ --> intent_analysis;
	__start__ --> memory_search;
	chat_agent -.-> chat_summary;
	chat_agent -.-> memory_creation;
	chat_agent -.-> tool_executor;
	chat_summary --> title_generation;
	dynamic_tool_collection --> tool_composer;
	engineering_agent --> chat_agent;
	intent_analysis --> dynamic_tool_collection;
	intent_analysis --> static_tool_collection;
	memory_creation --> memory_storage;
	memory_search --> workflow_router;
	search_summary --> chat_agent;
	static_tool_collection --> tool_composer;
	title_generation --> memory_creation;
	tool_composer --> workflow_router;
	tool_executor -.-> chat_agent;
	tool_executor -.-> search_summary;
	workflow_router -.-> chat_agent;
	workflow_router -.-> engineering_agent;
	memory_storage --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc

```
