{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "NestedImportTest",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Unique identifier"
        },
        "name": {
            "type": "string",
            "description": "Name field"
        },
        "messageType": {
            "$ref": "../../schemas/message_type.yaml",
            "description": "Reference to an enum in another file"
        },
        "chatMessage": {
            "$ref": "../../schemas/chat_message.yaml",
            "description": "Reference to another message that has its own references"
        }
    },
    "required": [
        "id",
        "name"
    ]
}